#! /bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
SOURCE_DIR="${DIR}/../py"

source "${DIR}/SET"

gcloud functions \
  deploy ${FUNCTION_NAME_HTTP} \
  --source=${SOURCE_DIR} \
  --runtime=python37 \
  --trigger-topic=${PUBSUB_TOPIC}