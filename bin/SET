#!/usr/bin/env bash
##################################################################################
#                        GCP and  Deployment SETTINGS
##################################################################################
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

#if [ -z $REGION ]; then
#  export REGION='us-west1'
#fi
#
#if [ -z $ZONE ]; then
#  export ZONE='us-west1-a'
#fi

export ZONE='us-west1-a'
export REGION='us-west1'

export CONFIG='demo-datacloud'

source "$DIR/SET_cgm"

alias cl='clear'
alias config='gcloud config configurations list'
alias activate="gcloud config configurations activate"

echo "Done ${BASH_SOURCE[0]}"




